# Pulse 03: REQUIREMENTS

Settled. Authored `docs/vtrace/REQUIREMENTS.md`: 15 requirements
(`REQ-001..015`) derived from MISSION + CONOPS, each tracing to a parent
`NEED-*`/`CON-*`/`OPS-*`; quality checklist; CONOPS trace review; 4 deferrals
(`DEF-001..004`). Role-review fixed point: added REQ-007 requiring the reliability
**dispatch basis** (dedicated vs shared host track) be named on each claim —
the rail analog of PYLON's contingency basis.

Validation: `proof check .`, `git diff --check`. Next: SPECIFICATION_BASELINE.
