# GAUGE — Agent Instructions

Read `README.md`, `PRODUCT_PLAN.md`, and `CLAUDE.md` first. `CLAUDE.md` holds the
canonical house rules and the analysis pipeline.

GAUGE is a research/design project for Rail 2.0 governed by the VTRACE V-model.
Advance the planning baseline in `docs/vtrace/` one deliverable at a time to a
`.roles` review fixed point. Do not write implementation code until a work package
is accepted. Keep claims bounded by evidence labels and citations.
